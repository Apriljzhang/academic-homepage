---
interface Props {
  class?: string;
  /** Decorative colour bar (inner pages). */
  accentBar?: boolean;
}

const { accentBar = false } = Astro.props;
---

<div
  class:list={[
    'w-full px-5 py-8 sm:px-8 sm:py-10 md:px-12 lg:px-16 xl:px-20 2xl:px-24',
    Astro.props.class,
  ]}
>
  {
    accentBar ? (
      <div class="mb-5 flex items-center gap-2" aria-hidden="true">
        <span class="h-2 w-16 rounded-full bg-main" />
        <span class="h-2 w-8 rounded-full bg-accent-purple" />
      </div>
    ) : null
  }
  <slot />
</div>
