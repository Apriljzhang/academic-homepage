---
import { teachingSubnav } from '../data/sectionNav';
import { cityUStaffProfileUrl } from '../data/profiles';

function normPath(pathname: string): string {
  const x = pathname.replace(/\/$/, '') || '/';
  return x;
}

const current = normPath(Astro.url.pathname);

function isCurrent(href: string): boolean {
  const target = normPath(new URL(href, Astro.url).pathname);
  return current === target;
}
---

<nav class="space-y-6 text-sm" aria-label="Teaching subsection">
  <div>
    <p class="mb-2 text-xs font-bold uppercase tracking-wider text-muted">Teaching</p>
    <ul class="flex flex-col gap-1.5">
      {
        teachingSubnav.map(({ href, label }) => (
          <li>
            <a
              href={href}
              aria-current={isCurrent(href) ? 'page' : undefined}
              class:list={[
                'block rounded-md px-2 py-1 no-underline transition-colors',
                isCurrent(href) ? 'bg-secondary-faint font-semibold text-ink' : 'text-muted hover:bg-neutral-hover hover:text-ink',
              ]}
            >
              {label}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
  <div>
    <p class="mb-2 text-xs font-bold uppercase tracking-wider text-muted">Institution</p>
    <a
      href={cityUStaffProfileUrl}
      target="_blank"
      rel="noopener noreferrer"
      class="block rounded-md px-2 py-1 text-muted no-underline hover:bg-neutral-hover hover:text-ink"
    >
      CityU SOE profile →
    </a>
  </div>
</nav>
