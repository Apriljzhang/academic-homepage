---
import { teachingOverviewCards } from '../data/cv';
---

<section class="mt-8" aria-label="Teaching overview">
  <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
    {
      teachingOverviewCards.map((card) => (
        <article class="rounded-lg border border-border bg-surface p-5">
          <h3 class="type-card-title text-secondary">{card.title}</h3>
          <div class="mt-3 space-y-4">
            {card.paragraphs.map((paragraph) => (
              <p class="type-body !max-w-none text-pretty text-ink/90">{paragraph}</p>
            ))}
          </div>
        </article>
      ))
    }
  </div>
</section>
